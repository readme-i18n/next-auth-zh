import { Callout } from "nextra/components"
import { Code } from "@/components/Code"

<img align="right" src="/img/providers/freshbooks.svg" height="64" width="64" />

# Freshbooks 提供商

## 相关资源

- [FreshBooks OAuth 文档](https://developer.freshbooks.com/docs/places-api/authentication/#web-applications)

## 配置

### 回调 URL

<Code>
  <Code.Next>

```bash
https://example.com/api/auth/callback/freshbooks
```

  </Code.Next>
  <Code.Qwik>

```bash
https://example.com/auth/callback/freshbooks
```

  </Code.Qwik>
  <Code.Svelte>

```bash
https://example.com/auth/callback/freshbooks
```

  </Code.Svelte>
</Code>

### 环境变量

```
AUTH_FRESHBOOKS_ID
AUTH_FRESHBOOKS_SECRET
```

### 配置

<Code>
  <Code.Next>

```ts filename="/auth.ts"
import NextAuth from "next-auth"
import FreshBooks from "next-auth/providers/freshbooks"

export const { handlers, auth, signIn, signOut } = NextAuth({
  providers: [FreshBooks],
})
```

  </Code.Next>
  <Code.Qwik>
  
```ts filename="/src/routes/plugin@auth.ts"
import { QwikAuth$ } from "@auth/qwik"
import FreshBooks from "@auth/qwik/providers/freshbooks"

export const { onRequest, useSession, useSignIn, useSignOut } = QwikAuth$(
  () => ({
    providers: [FreshBooks],
  })
)
```

  </Code.Qwik>
  <Code.Svelte>

```ts filename="/src/auth.ts"
import { SvelteKitAuth } from "@auth/sveltekit"
import FreshBooks from "@auth/sveltekit/providers/freshbooks"

export const { handle, signIn, signOut } = SvelteKitAuth({
  providers: [FreshBooks],
})
```

  </Code.Svelte>
  <Code.Express>

```ts filename="/src/app.ts"
import { ExpressAuth } from "@auth/express"
import FreshBooks from "@auth/express/providers/freshbooks"

app.use("/auth/*", ExpressAuth({ providers: [FreshBooks] }))
```

  </Code.Express>
</Code>

### 注意事项

- Freshbooks 要求在 YYYYMMDD 格式中额外提供 apiVersion 参数，该参数本质上表示"我已准备好接受截至该日期的 API 变更"。
